{
  "Logging": {
    "LogLevel": {

      //A seção Logging define as configurações de log da aplicação.
      //A configuração de logging controla o que será registrado e
      //em que nível, ajudando a monitorar o comportamento da aplicação.
      //Aqui, no LogLevel se especificam os níveis de log que a aplicação
      //deve capturar. O "Default" define o nível de log padrão da aplicação como "Information",
      //o que significa que mensagens com severidade de nível "Informação" ou superior serão registradas.
      //"Microsoft.AspNetCore": Define o nível de log específico para componentes do ASP.NET Core como
      //"Warning", o que significa que apenas mensagens de "Aviso" ou superiores serão registradas para
      //componentes internos do framework.
      //Trace: O nível mais detalhado.
      //Debug: Informações úteis para desenvolvimento e depuração.
      //Information: Informações gerais sobre o fluxo normal da aplicação.
      //Warning: Indica um possível problema.
      //Error: Quando algo deu errado.
      //Critical: Quando algo deu muito errado e a aplicação pode não se recuperar.

      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  //Este campo define quais hosts podem acessar a aplicação. O valor "*" permite que
  //qualquer host faça isso. Isso pode ser alterado para limitar o acesso a hosts específicos.
  "ConnectionStrings": {
    //ConnectionStrings contém informações de conexão para o banco de dados, que a aplicação
    //usa para se comunicar com o SQL Server.

    "ITSEsempiDB": "Server=localhost\\SQLEXPRESS;Database=ITS_CORSI_ESAMI;Trusted_Connection=True;TrustServerCertificate=True;"
    //"ITSEsempidb": O nome da string de conexão. Neste caso, está se conectando a uma instância
    //SQL Server e ao banco de dados usando "Trusted_Connection=True;", o que indica que a
    //autenticação do Windows será usada para se conectar ao banco de dados.
  }
}
