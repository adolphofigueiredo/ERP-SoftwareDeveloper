a = float(input('What is the height, in meters, of the wall? '))
l = float(input('What is the width, in meters, of the wall? '))
a2 = a * l
t = a2 / 2
print('You will need to buy {} liters of paint.'.format(t))
